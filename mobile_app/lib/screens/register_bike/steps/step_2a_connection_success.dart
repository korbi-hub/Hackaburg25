import 'package:flutter/material.dart';

class Step2aConnectionSuccess extends StatelessWidget {
  const Step2aConnectionSuccess({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
